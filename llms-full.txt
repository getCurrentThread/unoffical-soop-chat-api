# SOOPAPI - 비공식 SOOP 채팅 API (전체 레퍼런스)

> SOOP(한국 라이브 스트리밍 플랫폼) 채팅 시스템과 상호작용하기 위한 비공식 Java 25+ 라이브러리.
> WebSocket 기반 실시간 채팅 연결, 93개 이상의 타입 안전한 이벤트 디코딩, 이벤트 기반 아키텍처를 제공합니다.

## 핵심 정보

- GitHub: https://github.com/getCurrentThread/soopapi
- 버전: v0.14.0
- 라이선스: MIT
- 언어: Java 25 이상
- 빌드: Gradle 9.3.1
- 배포: JitPack (com.github.getCurrentThread:soopapi)
- 런타임 의존성: Gson

## 설치

### Gradle (JitPack)

```groovy
repositories {
    maven { url 'https://jitpack.io' }
}

dependencies {
    implementation 'com.github.getCurrentThread:soopapi:v0.13.0' // 최신 버전은 핵심 정보 참조
}
```

## 빠른 시작

### 통합 클라이언트 (SOOPClient) - 권장

```java
import com.github.getcurrentthread.soopapi.SOOPClient;
import com.github.getcurrentthread.soopapi.api.model.*;
import com.github.getcurrentthread.soopapi.client.SOOPChatClient;
import com.github.getcurrentthread.soopapi.event.ChatEvent;
import com.github.getcurrentthread.soopapi.event.model.*;

public class Example {
    public static void main(String[] args) throws Exception {
        try (SOOPClient client = new SOOPClient()) {
            // 방송/채널 정보 조회 (HTTP API)
            LiveDetail detail = client.live().detail("streamerId").join();
            System.out.println("방송 제목: " + detail.title());
            StationInfo station = client.channel().station("streamerId").join();
            System.out.println("스테이션: " + station.stationName());

            // 글로벌 리스너 — 이후 add()되는 모든 스트림에 자동 attach. 핸들러: (streamerId, event)
            client.on(ChatEvent.CHAT_MESSAGE, (bid, ChatMessageEvent e) -> {
                System.out.println(e.senderNickname() + ": " + e.message());
            });

            client.on(ChatEvent.SEND_BALLOON, (bid, SendBalloonEvent e) -> {
                System.out.println(e.senderNickname() + "님이 풍선 " + e.count() + "개 선물!");
            });

            // add() 호출 즉시 비동기 연결 — 별도 connectToChat() 불필요
            client.add("streamerId");

            // 모든 연결이 끊길 때까지 대기
            client.connectAll().join();
        }
    }
}
```

### 직접 연결 (익명/읽기 전용)

```java
SOOPChatConfig config = new SOOPChatConfig.Builder()
        .bid("streamerId")
        .build();

SOOPChatClient client = new SOOPChatClient(config);

client.on(ChatEvent.CHAT_MESSAGE, (ChatMessageEvent e) -> {
    System.out.println(e.senderNickname() + ": " + e.message());
});

client.connectAndAwait();
```

authCookie 없이 연결하면 익명 모드로 동작합니다. 채팅 수신은 가능하지만 sendChat() 호출 시 AuthenticationException이 발생합니다.

### 인증 + 채팅 전송

```java
SOOPClient client = new SOOPClient();

AuthCookie cookie = client.auth().signIn("userId", "password").join();

SOOPChatConfig config = new SOOPChatConfig.Builder()
        .bid("streamerId")
        .authCookie(cookie)
        .build();

SOOPChatClient chat = new SOOPChatClient(config);

chat.on(ChatEvent.CHAT_MESSAGE, (ChatMessageEvent e) -> {
    System.out.println(e.senderNickname() + ": " + e.message());
});

chat.connectToChat();
chat.sendChat("Hello!").join();
```

### 연결 상태 이벤트

```java
client.on(ChatEvent.DISCONNECTED, (DisconnectedEvent e) -> {
    System.out.println("연결 해제: code=" + e.statusCode()
            + ", reason=" + e.reason()
            + ", error=" + e.causedByError());
});

client.on(ChatEvent.RECONNECTING, (ReconnectingEvent e) -> {
    System.out.println("재연결 시도 " + e.attemptNumber()
            + "/" + e.maxAttempts()
            + " (" + e.delayMs() + "ms 후)");
});

client.on(ChatEvent.RECONNECTED, (ReconnectedEvent e) -> {
    System.out.println("재연결 완료 (총 " + e.totalAttempts() + "회 시도)");
});
```

### 에러 핸들링

```java
client.getEventEmitter().setErrorHandler((EventEmitterException ex) -> {
    System.err.println("이벤트 처리 중 오류: " + ex.getChatEvent());
    ex.printStackTrace();
});
```

## API 레퍼런스

### SOOPClient (파사드)

통합 진입점. AutoCloseable 구현.

- SOOPClient() - 기본 설정으로 생성
- SOOPClient(SOOPClientConfig config) - 커스텀 설정
- auth() -> SOOPAuth - 인증 API
- live() -> SOOPLive - 방송 정보 API
- channel() -> SOOPChannel - 채널 정보 API
- add(String streamerId) -> SOOPChatClient - 등록 + 즉시 비동기 연결. bid 기준 dedup. 자동 재연결 지원.
- add(SOOPChatConfig config) -> SOOPChatClient - 커스텀 설정으로 등록 + 자동 연결.
- remove(String streamerId) -> boolean - 개별 disconnect + 등록 해제. 등록돼 있었으면 true.
- get(String streamerId) -> SOOPChatClient - 등록된 핸들 반환(없으면 null).
- streamerIds() -> Set<String> - 등록된 bid 스냅샷.
- clients() -> Collection<SOOPChatClient> - 등록된 클라이언트 스냅샷.
- <T extends BaseEvent> on(ChatEvent, StreamEventListener<T>) -> SOOPClient - 글로벌 리스너 등록. 모든 스트림 + 이후 추가되는 스트림에 자동 attach. 핸들러: (streamerId, event).
- <T extends BaseEvent> off(ChatEvent, StreamEventListener<T>) -> SOOPClient - 글로벌 리스너 해제.
- connectAll() -> CompletableFuture<Void> - 등록된 모든 클라이언트의 disconnect를 대기.
- reconnect(String streamerId) -> CompletableFuture<Void> - tear-down + 새 연결로 강제 재연결. 현재 상태/진행 중 backoff 무시. RECONNECTING/DISCONNECTED/RECONNECTED 이벤트 emit. 미등록 bid면 IllegalArgumentException으로 실패.
- reconnectAll() -> CompletableFuture<Void> - 등록된 모든 스트림에 강제 재연결 수행 후 모두 disconnect될 때까지 대기.
- chat(String streamerId) -> SOOPChatClient - add()의 별칭(편의 메서드).
- chat(SOOPChatConfig config) -> SOOPChatClient - add()의 별칭(편의 메서드).
- close() - 모든 리소스 해제. try-with-resources 권장.

### SOOPChatClient (채팅 클라이언트)

채팅 연결 및 이벤트 구독. AutoCloseable 구현.

이벤트 관리:
- <T extends BaseEvent> on(ChatEvent event, EventListener<T> listener) -> SOOPChatClient - 이벤트 구독
- <T extends BaseEvent> once(ChatEvent event, EventListener<T> listener) -> SOOPChatClient - 일회성 구독
- <T extends BaseEvent> off(ChatEvent event, EventListener<T> listener) -> SOOPChatClient - 구독 해제

연결:
- connectToChat() -> CompletableFuture<Void> - 비동기 연결 (연결 해제 시 완료)
- connectAndAwait() - 블로킹 연결 (연결 해제까지 대기)
- disconnect() - 연결 해제
- reconnect() -> CompletableFuture<Void> - 정상 연결 상태에서의 가벼운 재연결. 초기 미연결이면 실패.
- forceReconnect() -> CompletableFuture<Void> - tear-down + 새 연결. 현재 상태/backoff 무시. RECONNECTING/RECONNECTED emit.
- isConnected() -> boolean - 연결 상태 확인
- getConnectionStatus() -> CompletableFuture<ConnectionStatus> - 상세 연결 상태

채팅:
- sendChat(String message) -> CompletableFuture<Void> - 채팅 전송 (인증 필요)

기타:
- getBid() -> String - 방송인 ID
- getEventEmitter() -> EventEmitter - 이벤트 에미터 직접 접근

### SOOPChatConfig.Builder (설정)

| 메서드 | 타입 | 기본값 | 설명 |
|--------|------|--------|------|
| bid(String) | String | (필수) | 방송인 ID |
| bno(String) | String | null (자동 해석) | 방송 번호 |
| authCookie(AuthCookie) | AuthCookie | null (익명 모드) | 인증 쿠키 |
| connectionTimeout(Duration) | Duration | 30초 | 연결 타임아웃 |
| maxRetryAttempts(int) | int | 5 | 최대 재시도 횟수 |
| pingIntervalSeconds(long) | long | 60 | 핑 전송 간격(초) |
| initialPacketDelayMs(long) | long | 1000 | 초기 패킷 대기(ms) |
| sslContext(SSLContext) | SSLContext | null | 커스텀 SSL 컨텍스트 |

### SOOPAuth (인증)

- signIn(String userId, String password) -> CompletableFuture<AuthCookie>

AuthCookie record 필드: userId, success, rawResponse, authTicket, abroadChk, abroadVod, bbsTicket, rdb, userTicket, au, au3rd, ausa, ausb
- isAuthenticated() -> boolean

### SOOPLive (방송 정보)

- getBno(String streamerId) -> CompletableFuture<String>
- detail(String streamerId) -> CompletableFuture<LiveDetail>
- detail(String streamerId, String bno) -> CompletableFuture<LiveDetail>
- detail(String streamerId, String bno, AuthCookie authCookie) -> CompletableFuture<LiveDetail>

LiveDetail record 필드: bjId, bno, title, chatDomain, chatNo, ftk, chatPort, result, bps, geoCC, geoRC, acptLang, svcLang

### SOOPChannel (채널 정보)

- station(String streamerId) -> CompletableFuture<StationInfo>

StationInfo record 필드: userId, userNickname, stationNo, stationName, stationTitle, isLive, totalFollowers

## 이벤트 시스템

### 이벤트 계층 (Sealed Interface)

BaseEvent (sealed)
├── ChatBaseEvent - 채팅 관련 (메시지, 입퇴장)
├── DonationBaseEvent - 후원 관련 (풍선, 초콜릿, 구독)
├── SystemBaseEvent - 시스템 (연결, 서버 상태)
├── ModerationBaseEvent - 관리 (킥, 채금, 차단)
├── ItemBaseEvent - 아이템 (구매, 드롭)
├── NotificationBaseEvent - 알림 (공지, 미션)
└── UnknownEvent - 알 수 없는 타입

모든 이벤트는 Java Record이며 공통 필드: eventType(), raw(), timestamp()

### EventEmitter

- on(ChatEvent, EventListener) - 이벤트 구독
- once(ChatEvent, EventListener) - 일회성 구독
- off(ChatEvent, EventListener) - 구독 해제
- setErrorHandler(Consumer<EventEmitterException>) - 에러 핸들러 설정
- clear() - 모든 리스너 제거
- clear(ChatEvent) - 특정 이벤트의 리스너 제거
- hasListeners(ChatEvent) -> boolean

## connectToChat() 동작 방식

v0.5.0부터 connectToChat()이 반환하는 CompletableFuture는 연결이 해제될 때 완료됩니다.

| 메서드 | 동작 |
|--------|------|
| connectToChat() | 연결 해제 시 완료되는 CompletableFuture<Void> 반환 |
| connectToChat().join() | 연결이 해제될 때까지 현재 스레드를 블로킹 |
| connectAndAwait() | connectToChat().join()의 편의 메서드 (블로킹) |

## 에러 처리

### 예외 계층

SOOPChatException (RuntimeException)
├── AuthenticationException - 로그인 실패, 미인증 sendChat()
├── ConnectionException - WebSocket/HTTP 연결 실패
└── EventEmitterException - 이벤트 리스너 예외 래핑

EventEmitterException 추가 메서드: getChatEvent() -> ChatEvent

---

## 아키텍처 개요 (기여자용)

### 패키지 구조

```
lib/src/main/java/com/github/getcurrentthread/soopapi/
├── SOOPClient.java                          # 파사드 진입점
├── api/                                     # HTTP API 통신
│   ├── SOOPAuth.java                        #   로그인/인증
│   ├── SOOPLive.java                        #   방송 정보 API
│   ├── SOOPChannel.java                     #   채널 정보 API
│   ├── SOOPHttpClient.java                  #   공유 HTTP 클라이언트
│   └── model/                               #   데이터 모델 (AuthCookie, LiveDetail, StationInfo)
├── client/                                  # 사용자 대면 클라이언트
│   └── SOOPChatClient.java                  #   이벤트 구독 + 연결 관리
├── config/                                  # 설정
│   ├── SOOPChatConfig.java                  #   채팅 연결 설정 (Builder)
│   └── SOOPClientConfig.java                #   전역 클라이언트 설정 (Builder)
├── connection/                              # WebSocket 연결 관리
│   ├── ConnectionManager.java               #   싱글톤 연결 매니저
│   └── SOOPConnection.java                  #   채널별 연결 핸들러
├── decoder/                                 # 메시지 파싱/디스패치
│   ├── MessageDispatcher.java               #   메시지 라우팅
│   ├── message/                             #   개별 디코더 (87개)
│   │   ├── IMessageDecoder.java             #     디코더 인터페이스
│   │   ├── ChatMessageDecoder.java          #     채팅 메시지 디코더 (대표 예시)
│   │   └── ...                              #     SendBalloonDecoder, ChocolateDecoder 등
│   └── factory/                             #   디코더 팩토리
│       ├── MessageDecoderFactory.java        #     팩토리 인터페이스
│       └── DefaultMessageDecoderFactory.java #     87개 디코더 등록
├── event/                                   # 이벤트 시스템
│   ├── ChatEvent.java                       #   93개 이벤트 열거형
│   ├── EventEmitter.java                    #   Pub/Sub 이벤트 버스
│   ├── EventListener.java                   #   리스너 함수형 인터페이스
│   └── model/                               #   이벤트 Record 타입 (104개)
│       ├── BaseEvent.java                   #     sealed 루트 인터페이스
│       ├── ChatBaseEvent.java               #     채팅 카테고리
│       ├── DonationBaseEvent.java           #     후원 카테고리
│       ├── SystemBaseEvent.java             #     시스템 카테고리
│       ├── ModerationBaseEvent.java         #     관리 카테고리
│       ├── ItemBaseEvent.java               #     아이템 카테고리
│       ├── NotificationBaseEvent.java       #     알림 카테고리
│       └── UnknownEvent.java                #     미지의 이벤트
├── code/                                    # 소켓 코드표
│   ├── UserFlag.java                        #   사용자 등급 비트 - 주 그룹
│   ├── UserFlag2.java                       #   사용자 등급 비트 - 보조 그룹
│   ├── UserLevel.java                       #   "주|보조" 파싱 컨테이너 (record)
│   ├── ChatIceType.java                     #   아이스/제한 모드 (+ 중첩 Flag)
│   └── ChatQuitStatus.java                  #   채널 퇴장 사유
├── exception/                               # 예외
│   ├── SOOPChatException.java               #   기본 예외 (RuntimeException)
│   ├── AuthenticationException.java
│   ├── ConnectionException.java
│   └── EventEmitterException.java
├── model/                                   # 도메인 모델
│   ├── ChannelInfo.java                     #   WebSocket 엔드포인트 정보
│   └── ConnectionStatus.java                #   연결 상태 스냅샷
├── constant/                                # 프로토콜 상수
│   └── SOOPConstants.java                   #   패킷 구분자, ESC 헤더
├── util/                                    # 유틸리티
│   ├── SOOPChatUtils.java                   #   파싱, UTF-8, 예외 언래핑
│   ├── GsonUtil.java                        #   JSON 직렬화
│   └── SSLContextProvider.java              #   SSL/TLS 컨텍스트
└── websocket/                               # WebSocket 계층
    ├── WebSocketManager.java                #   연결/재연결/핑 관리
    ├── WebSocketListener.java               #   수신 메시지 처리
    └── WebSocketPacketBuilder.java          #   패킷 직렬화
```

### 핵심 컴포넌트 관계

```
SOOPClient (파사드)
├── SOOPAuth ─── SOOPHttpClient
├── SOOPLive ─── SOOPHttpClient
├── SOOPChannel ─ SOOPHttpClient
└── SOOPChatClient
    └── ConnectionManager (싱글톤)
        └── SOOPConnection (채널당 1개)
            ├── WebSocketManager ─── WebSocketListener
            └── MessageDispatcher ─── IMessageDecoder[]
                                      └── EventEmitter ─── 사용자 리스너
```

### 메시지 처리 파이프라인

```
WebSocket.onText()
    → WebSocketListener.onText(): textBuffer에 프래그먼트 조립
    → last=true → MessageDispatcher.dispatchMessage()
    → Virtual Thread에서 실행:
        1. RAW 리스너 확인 → RawEvent 발행 (있을 때만)
        2. F_CHAR로 첫 구분자 찾기 → 헤더 추출
        3. parseServiceCode() → ChatEvent.fromCode()
        4. hasListeners() 확인 (없으면 조기 반환)
        5. IMessageDecoder.decode(parts[], raw) → BaseEvent
        6. EventEmitter.emit(chatEvent, event)
            → internalListeners 먼저 호출
            → listeners 호출
```

## 내부 구조 상세

### ConnectionManager (싱글톤)

파일: connection/ConnectionManager.java

- Initialization-on-demand Holder(IODH) 패턴을 사용한 lazy 싱글톤
- messageProcessorPool: Executors.newVirtualThreadPerTaskExecutor() — 메시지당 Virtual Thread 생성
- sharedScheduler: ScheduledThreadPool(1, daemon) — 핑/재연결 스케줄링
- connections: ConcurrentHashMap<String, SOOPConnection> — BID별 연결 관리
- JVM shutdown hook으로 모든 연결 정리
- shutdown(): newVirtualThreadPerTaskExecutor() + try-with-resources로 모든 연결 병렬 종료

### SOOPConnection (채널별 연결)

파일: connection/SOOPConnection.java

- SHARED_DECODERS: static final Map — 모든 연결에서 디코더 인스턴스 공유 (메모리 최적화)
- connect() 흐름:
  1. getBno() → BNO가 없으면 API로 조회
  2. soopLive.detail(bid, bno) → LiveDetail 획득
  3. toChannelInfo() → ChannelInfo(CHATNO, FTK, CHPT, CHDOMAIN 등) 변환
  4. CHPT/CHDOMAIN 유효성 검증
  5. webSocketManager.connect(channelInfo)
- registerEnterInfoHandler(): 인증 연결 시 JOIN_CHANNEL 이벤트의 userFlag(synAck)를 ENTER_INFO 패킷으로 전송
- ReentrantLock으로 연결 상태 보호

### WebSocketManager

파일: websocket/WebSocketManager.java

- WsState record: (connected, ws, ping) — AtomicReference로 원자적 상태 관리
- connect(): buildWebSocketUri() → httpClient.newWebSocketBuilder() → sendInitialPackets() → startPingScheduler()
- 재연결: scheduleRetry() — 지수 백오프 (2s, 4s, 8s, ... 최대 30s)
- RECONNECTING 이벤트 발행 (시도 전) → RECONNECTED 이벤트 발행 (성공 시)
- Ping: pingIntervalSeconds 간격으로 핑 패킷 전송, 실패 시 자동 재연결

### WebSocketListener

파일: websocket/WebSocketListener.java

- onText(): StringBuilder textBuffer로 프래그먼트 조립
- last=true일 때 MessageDispatcher.dispatchMessage() 호출
- 버퍼 관리: 기본 16KB, 4배(64KB) 초과 시 trimToSize()
- onClose()/onError(): DISCONNECTED 이벤트 발행

## WebSocket 프로토콜

### 패킷 구조

```
[ESC][command:4][length:6][suffix:2][payload]

ESC     = \u001b\t (2바이트: ESC + TAB)
command = 4자리 문자열 서비스 코드
length  = UTF-8 바이트 길이, 6자리 0-패딩
suffix  = "00" (고정)
payload = F(\u000c) 구분자로 필드 분리
```

### 명령 코드 (WebSocketPacketBuilder)

| 코드 | 상수 | 용도 |
|------|------|------|
| 0000 | CMD_PING | 연결 유지 핑 |
| 0001 | CMD_CONNECT | 초기 연결 핸드셰이크 |
| 0002 | CMD_JOIN | 채팅 채널 입장 |
| 0005 | CMD_CHAT | 채팅 메시지 전송 |
| 0012 | CMD_ENTER_INFO | 인증된 사용자 입장 정보 |

### 필드 구분자 (SOOPConstants)

| 상수 | 값 | 용도 |
|------|------|------|
| F | \u000c | 기본 필드 구분자 (폼 피드) |
| F_CHAR | \u000c | char 타입 (성능 최적화) |
| ESC | \u001b\t | 패킷 헤더 접두사 |
| ELEMENT_START | \u0011 | 메타데이터 블록 시작 |
| ELEMENT_END | \u0012 | 메타데이터 블록 끝 |
| SPACE | \u0006 | 메타데이터 내 연산자 공백 |

### 연결 핸드셰이크 시퀀스

1. Connect 패킷 (CMD_CONNECT=0001):
   - 익명: F×3 + "16" + F
   - 인증: F + AuthTicket + F×2 + "16" + F

2. 대기 (initialPacketDelayMs, 기본 1000ms)

3. Join 패킷 (CMD_JOIN=0002):
   - 익명: F + CHATNO + F×5
   - 인증: F + CHATNO + F + FTK + F + "0" + F + log{...}pwd{...}auth_info{NULL}pver{2}access_system{html5} + F

4. [인증 시] EnterInfo 패킷 (CMD_ENTER_INFO=0012):
   - JOIN_CHANNEL 응답의 userFlag(synAck) 수신 후 전송
   - F + synAck + F + "0" + F

### Ping 메커니즘

- pingIntervalSeconds (기본 60초) 간격으로 CMD_PING 패킷 전송
- 전송 실패 시 자동 재연결 시도

## 디코더 시스템

### IMessageDecoder 인터페이스

```java
@FunctionalInterface
public interface IMessageDecoder {
    BaseEvent decode(String[] parts, String raw);
}
```

### DefaultMessageDecoderFactory

파일: decoder/factory/DefaultMessageDecoderFactory.java

87개 디코더를 Map.ofEntries()로 등록. static final로 선언되어 모든 SOOPConnection에서 공유.

### 디코더 구현 패턴 (ChatMessageDecoder 예시)

```java
public class ChatMessageDecoder implements IMessageDecoder {
    private static final int MIN_PARTS = 8;

    @Override
    public BaseEvent decode(String[] parts, String raw) {
        if (parts.length < MIN_PARTS) {
            return null;  // 필드 부족 시 무시
        }
        return new ChatMessageEvent(
                parts[0],                              // message
                parts[1],                              // senderId
                SOOPChatUtils.safeParseInt(parts[3], 0), // type
                SOOPChatUtils.safeParseInt(parts[4], 0), // chatLang
                parts[5],                              // senderNickname
                parts[6],                              // senderFlag
                parts[7],                              // subscriptionMonth
                parts.length > 8 ? parts[8] : "",      // randomNicknameColor (선택)
                parts.length > 9 ? parts[9] : "",      // randomNicknameColorDarkmode (선택)
                ChatEvent.CHAT_MESSAGE,
                raw,
                System.currentTimeMillis());
    }
}
```

규칙:
1. MIN_PARTS로 최소 필드 수 검증
2. parts[] 인덱스로 필드 매핑
3. SOOPChatUtils.safeParseInt()로 안전한 정수 파싱
4. 선택적 필드: parts.length > N ? parts[N] : ""
5. 공통 3개 필드 항상 포함: ChatEvent, raw, timestamp

### MessageDispatcher 처리 흐름

파일: decoder/MessageDispatcher.java

1. null/빈 메시지 무시
2. Virtual Thread에서 비동기 실행:
   a. RAW 리스너 확인 → 있으면 RawEvent 발행 (없으면 할당 스킵)
   b. F_CHAR로 첫 구분자 인덱스 찾기 → 없으면 반환
   c. 헤더 추출 → parseServiceCode() → ChatEvent.fromCode()
   d. hasListeners(chatEvent) 확인 → 없으면 조기 반환 (디코딩 스킵)
   e. 디코더 조회 → 없으면 UnknownEvent 생성
   f. decoder.decode(parts, raw) → 결과가 null이면 무시
   g. eventEmitter.emit(chatEvent, event)

## 전체 이벤트 목록

### 기본 연결 (0~2)

| 코드 | 이벤트 | 설명 |
|------|--------|------|
| 0 | KEEP_ALIVE | 핑퐁 |
| 1 | LOGIN | 로그인 핸드셰이크 |
| 2 | JOIN_CHANNEL | 채널 입장 핸드셰이크 |

### 채팅 / 사용자 (3~9)

| 코드 | 이벤트 | 설명 |
|------|--------|------|
| 3 | QUIT_CHANNEL | 채널 퇴장 |
| 4 | CHAT_USER | 사용자 입퇴장 |
| 5 | CHAT_MESSAGE | 채팅 메시지 |
| 6 | SET_CHANNEL_NAME | 채널 이름 설정 |
| 7 | SET_BJ_STAT | BJ 상태 설정 |
| 8 | SET_DUMB | 채금 |
| 9 | DIRECT_CHAT | 귓속말 |

### 공지 / 관리 (10~17)

| 코드 | 이벤트 | 설명 |
|------|--------|------|
| 10 | NOTICE | 공지 |
| 11 | KICK | 강제 퇴장 |
| 12 | SET_USER_FLAG | 사용자 플래그 설정 |
| 13 | SET_SUB_BJ | 서브 BJ 설정 |
| 14 | SET_NICKNAME | 닉네임 설정 |
| 15 | SERVER_STAT | 서버 상태 |
| 16 | NULL_16 | 미사용 |
| 17 | CLUB_COLOR | 클럽 색상 |

### 후원 / 별풍선 (18~22)

| 코드 | 이벤트 | 설명 |
|------|--------|------|
| 18 | SEND_BALLOON | 별풍선 후원 |
| 19 | ICE_MODE | 아이스 모드 |
| 20 | SEND_FAN_LETTER | 팬레터 전송 |
| 21 | ICE_MODE_EX | 확장 아이스 모드 |
| 22 | GET_ICE_MODE_RELAY | 아이스 모드 릴레이 |

### 채팅 제어 (23~27)

| 코드 | 이벤트 | 설명 |
|------|--------|------|
| 23 | SLOW_MODE | 슬로우 모드 |
| 24 | RELOAD_BURN_LEVEL | 번 레벨 리로드 |
| 25 | BLIND_KICK | 블라인드 킥 |
| 26 | MANAGER_CHAT | 매니저 채팅 |
| 27 | APPEND_DATA | 데이터 추가 |

### 이벤트 / 아이템 (28~48)

| 코드 | 이벤트 | 설명 |
|------|--------|------|
| 28 | BASEBALL_EVENT | 야구 이벤트 |
| 29 | PAID_ITEM | 유료 아이템 |
| 30 | TOP_FAN | 열혈 팬 |
| 31 | SNS_MESSAGE | SNS 메시지 |
| 32 | SNS_MODE | SNS 모드 |
| 33 | SEND_BALLOON_SUB | 별풍선 (서브) |
| 34 | SEND_FAN_LETTER_SUB | 팬레터 (서브) |
| 35 | TOP_FAN_SUB | 열혈 팬 (서브) |
| 36 | BJ_STICKER_ITEM | BJ 스티커 아이템 |
| 37 | CHOCOLATE | 초콜릿 후원 |
| 38 | CHOCOLATE_SUB | 초콜릿 (서브) |
| 39 | TOP_CLAN | 탑 클랜 |
| 40 | TOP_CLAN_SUB | 탑 클랜 (서브) |
| 41 | SUPER_CHAT | 슈퍼챗 |
| 42 | UPDATE_TICKET | 티켓 업데이트 |
| 43 | NOTI_GAME_RANKER | 게임 랭커 알림 |
| 44 | STAR_COIN | 스타 코인 |
| 45 | SEND_QUICK_VIEW | 퀵뷰 선물 |
| 46 | ITEM_STATUS | 아이템 상태 |
| 47 | ITEM_USING | 아이템 사용 중 |
| 48 | USE_QUICK_VIEW | 퀵뷰 사용 |

### 투표 / 차단 / 방송 정보 (50~58)

| 코드 | 이벤트 | 설명 |
|------|--------|------|
| 50 | NOTIFY_POLL | 투표 알림 |
| 51 | CHAT_BLOCK_MODE | 채팅 차단 모드 |
| 52 | BDM_ADD_BLACK_INFO | 블랙리스트 추가 |
| 53 | SET_BROAD_INFO | 방송 정보 설정 |
| 54 | BAN_WORD | 금지어 설정 |
| 58 | SEND_ADMIN_NOTICE | 관리자 공지 |

### 프리캣 / 상품 / 프로모션 (65~75)

| 코드 | 이벤트 | 설명 |
|------|--------|------|
| 65 | FREECAT_OWNER_JOIN | 프리캣 오너 입장 |
| 70 | BUY_GOODS | 상품 구매 |
| 71 | BUY_GOODS_SUB | 상품 구매 (서브) |
| 72 | SEND_PROMOTION | 프로모션 |
| 74 | NOTIFY_VR | VR 알림 |
| 75 | NOTIFY_MOBBROAD_PAUSE | 모바일 방송 일시정지 |

### 강퇴 / 관리자 (76~79)

| 코드 | 이벤트 | 설명 |
|------|--------|------|
| 76 | KICK_AND_CANCEL | 강퇴 + 취소 |
| 77 | KICK_USERLIST | 강퇴 사용자 목록 |
| 78 | ADMIN_CHAT_USER | 관리자 채팅 사용자 |
| 79 | CLI_DOBAE_INFO | 스팸 정보 |

### 후원 / 애드콘 (86~87)

| 코드 | 이벤트 | 설명 |
|------|--------|------|
| 86 | VOD_BALLOON | VOD 풍선 |
| 87 | ADCON_EFFECT | 애드콘 효과 |

### 구독 / 번역 (90~95)

| 코드 | 이벤트 | 설명 |
|------|--------|------|
| 90 | KICK_MSG_STATE | 강퇴 메시지 상태 |
| 91 | FOLLOW_ITEM | 신규 구독 |
| 92 | ITEM_SELL_EFFECT | 아이템 판매 효과 |
| 93 | FOLLOW_ITEM_EFFECT | 연속 구독 |
| 94 | TRANSLATION_STATE | 번역 상태 |
| 95 | TRANSLATION | 번역 |

### 티켓 / 공지 / 영상 후원 (102~109)

| 코드 | 이벤트 | 설명 |
|------|--------|------|
| 102 | GIFT_TICKET | 선물 티켓 |
| 103 | VOD_ADCON | VOD 애드콘 |
| 104 | BJ_NOTICE | BJ 공지 |
| 105 | VIDEO_BALLOON | 영상 후원 |
| 107 | STATION_ADCON | 스테이션 애드콘 |
| 108 | SEND_SUBSCRIPTION | 구독 선물 |
| 109 | OGQ_EMOTICON | OGQ 이모티콘 |

### 아이템 / 이모티콘 / 광고 (110~122)

| 코드 | 이벤트 | 설명 |
|------|--------|------|
| 110 | EMOTICON_TICKET | 이모티콘 티켓 |
| 111 | ITEM_DROPS | 아이템 드롭 |
| 117 | VIDEO_BALLOON_LINK | 영상 풍선 링크 |
| 118 | OGQ_EMOTICON_GIFT | OGQ 이모티콘 선물 |
| 119 | AD_IN_BROAD_JSON | 방송 내 광고 JSON |
| 120 | GEM_ITEM_SEND | 보석 아이템 전송 |
| 121 | MISSION | 챌린지 미션 |
| 122 | LIVE_CAPTION | 실시간 자막 |

### 미션 / 관리자 / 사용자 확장 (125~128)

| 코드 | 이벤트 | 설명 |
|------|--------|------|
| 125 | MISSION_SETTLE | 미션 정산 |
| 126 | SET_ADMIN_FLAG | 관리자 플래그 설정 |
| 127 | CHUSER_EXTEND | 구독자 목록 |
| 128 | ADMIN_CHUSER_EXTEND | 관리자 채팅 사용자 확장 |

### 특수 이벤트

| 코드 | 이벤트 | 설명 |
|------|--------|------|
| -2 | RAW | 모든 원시 패킷 |
| -3 | DISCONNECTED | 연결 해제 |
| -4 | RECONNECTING | 재연결 시도 |
| -5 | RECONNECTED | 재연결 완료 |
| -1 | NONE_TYPE | 알 수 없는 타입 |

## 이벤트 Record 필드 상세

모든 Record의 공통 필드: ChatEvent eventType, String raw, long timestamp
아래 필드 목록에서는 공통 필드를 생략하고 이벤트 고유 필드만 표기합니다.

### ChatBaseEvent (채팅)

CHAT_MESSAGE (5) — ChatMessageEvent
  String message, String senderId, int type, int chatLang, String senderNickname, String senderFlag, String subscriptionMonth, String randomNicknameColor, String randomNicknameColorDarkmode

DIRECT_CHAT (9) — DirectChatEvent
  String message, String senderId, String receiverId, int type, String senderNickname, String receiverNickname, String flag

MANAGER_CHAT (26) — ManagerChatEvent
  String message, String senderId, int isAdmin, int chatLang, String senderNickname, String senderFlag, String subscriptionMonth

SNS_MESSAGE (31) — SnsMessageEvent
  String message

SUPER_CHAT (41) — SuperChatEvent
  (공통 필드만)

### DonationBaseEvent (후원)

SEND_BALLOON (18) — SendBalloonEvent
  String bjId, String senderId, String senderNickname, int count, int fanOrder, String fileName, boolean isDefault, int isTopFan, String ttsData

SEND_BALLOON_SUB (33) — SendBalloonSubEvent
  String bjId, String senderId, String senderNickname, int count, int fanOrder, String fileName, boolean isDefault, int isTopFan

SEND_FAN_LETTER (20) — SendFanLetterEvent
  String bjId, String bjNickname, String senderId, String senderNickname, int type, int count, String supporterOrder

SEND_FAN_LETTER_SUB (34) — SendFanLetterSubEvent
  String bjId, String bjNickname, String senderId, String senderNickname, int type, int count, String supporterOrder

CHOCOLATE (37) — ChocolateEvent
  String bjId, String senderId, String senderNickname, int count

CHOCOLATE_SUB (38) — ChocolateEvent
  String bjId, String senderId, String senderNickname, int count

SEND_SUBSCRIPTION (108) — SendSubscriptionEvent
  String senderId, String senderNickname, String receiverId, String receiverNickname, String subscriptionId, String subscriptionNickname, int itemType, String itemCode, int isSubscription, String subscriptionType, String subscriptionPeriod, int subscriptionRemain, int subscriptionPaycount

VIDEO_BALLOON (105) — VideoBalloonEvent
  String chatNo, String bjId, String userId, String userNickname, int balloonCount, int fanOrder, int isTopFan, String relay, String fileName, boolean isDefault, String extraData

VOD_BALLOON (86) — VodBalloonEvent
  String bjId, String senderId, String senderNickname, int balloonCount

ADCON_EFFECT (87) — AdconEffectEvent
  int chatNo, String bjId, String senderId, String senderNickname, String message, String message2, String title, String urlImg, String urlDefault, int adconCount, int fanOrder, int isTopFan, int isFanChief, int isSubRoom

STATION_ADCON (107) — StationAdconEvent
  String bjId, String userId, String userNickName, int adconCount, String isDefault, String adconMsg, String chatNumber

GIFT_TICKET (102) — GiftTicketEvent
  String senderId, String senderNickname, String receiverId, String receiverNickname, String ticketData

OGQ_EMOTICON_GIFT (118) — GiftOGQEmoticonEvent
  String senderId, String senderNick, String receivedId, String receivedNick, String ogqTitle, String ogqImageUrl

GEM_ITEM_SEND (120) — GemItemSendEvent
  String receiverId, String receiverNick, String itemName

STAR_COIN (44) — StarCoinEvent
  (공통 필드만)

SEND_PROMOTION (72) — SendPromotionEvent
  (공통 필드만)

TOP_FAN (30) — TopFanEvent
  (공통 필드만)

TOP_FAN_SUB (35) — TopFanSubEvent
  (공통 필드만)

TOP_CLAN (39) — TopClanEvent
  (공통 필드만)

TOP_CLAN_SUB (40) — TopClanSubEvent
  (공통 필드만)

VIDEO_BALLOON_LINK (117) — VideoBalloonLinkEvent
  (공통 필드만)

VOD_ADCON (103) — VodAdconEvent
  (공통 필드만)

### SystemBaseEvent (시스템)

KEEP_ALIVE (0) — KeepAliveEvent
  (공통 필드만)

LOGIN (1) — LoginEvent
  String userId, String userFlag

JOIN_CHANNEL (2) — JoinChannelEvent
  String chatNo, String bjId, int maxSubBjCount, String familyNickname, String userFlag

QUIT_CHANNEL (3) — QuitChannelEvent
  int quitType, int adminKickCount, String nickname, String bannedRoomBjId, String bannedRoomBjNickname

CHAT_USER (4) — ChatUserEvent
  int type, List<ChatUserEntry> userList

SET_CHANNEL_NAME (6) — SetChannelNameEvent
  String channelName

SET_BJ_STAT (7) — SetBjStatEvent
  (공통 필드만)

ICE_MODE (19) — IceModeEvent
  int iceMode

ICE_MODE_EX (21) — IceModeExEvent
  int iceMode, int freezeType, int balloonLimitCount, int subscriptionLimitCount

GET_ICE_MODE_RELAY (22) — GetIceModeRelayEvent
  int iceMode, int freezeType

SET_USER_FLAG (12) — SetUserFlagEvent
  String oldFlag, String userId, String userNickname, String newFlag

SET_SUB_BJ (13) — SetSubBjEvent
  String userId, String flag, int hide, String nickname

SET_NICKNAME (14) — SetNicknameEvent
  String userId, String newNickname, int changeType, String flag, String oldNickname

SERVER_STAT (15) — ServerStatEvent
  String serverStat

NULL_16 (16) — Null16Event
  (공통 필드만)

CLUB_COLOR (17) — ClubColorEvent
  (공통 필드만)

APPEND_DATA (27) — AppendDataEvent
  String appendData

FREECAT_OWNER_JOIN (65) — FreecatOwnerJoinEvent
  String chatNo, String bjId, int maxSubBjCount, String familyNickname, String userFlag

SNS_MODE (32) — SnsModeEvent
  int snsMode

RELOAD_BURN_LEVEL (24) — ReloadBurnLevelEvent
  int burnLevel

EMOTICON_TICKET (110) — EmoticonTicketEvent
  int value

CHAT_BLOCK_MODE (51) — ChatBlockModeEvent
  (공통 필드만)

SET_BROAD_INFO (53) — SetBroadInfoEvent
  (공통 필드만)

BDM_ADD_BLACK_INFO (52) — BdmAddBlackInfoEvent
  (공통 필드만)

NOTIFY_MOB_BROAD_PAUSE (75) — NotifyMobBroadPauseEvent
  int status

NOTIFY_VR (74) — NotifyVrEvent
  int action, String bjId, String vrId, String rtmp, String hls, int vrType

NOTI_GAME_RANKER (43) — NotiGameRankerEvent
  (공통 필드만)

CHUSER_EXTEND (127) — ChuserExtendEvent
  Map<String, Map<String, Integer>> userStatus

DISCONNECTED (-3) — DisconnectedEvent
  int statusCode, String reason, boolean causedByError

RECONNECTING (-4) — ReconnectingEvent
  int attemptNumber, int maxAttempts, long delayMs

RECONNECTED (-5) — ReconnectedEvent
  int totalAttempts

NONE_TYPE (-1) — NoneTypeEvent
  int value

RAW (-2) — RawEvent
  (공통 필드만, raw에 원시 패킷 전체 포함)

### ModerationBaseEvent (관리)

SET_DUMB (8) — SetDumbEvent
  String userId, String userInfo, int dumbTime, int dumbCount, String adminId, int adminType, String extraInfo, String userNickname

KICK (11) — KickEvent
  String userId, String userNickname, int kickType, String reason, String kickerNickname

BLIND_KICK (25) — BlindKickEvent
  String userId, String userNickname, int blindTime

BAN_WORD (54) — BanWordEvent
  String replaceWord, String[] banWordList

SEND_ADMIN_NOTICE (58) — SendAdminNoticeEvent
  String message

KICK_AND_CANCEL (76) — KickAndCancelEvent
  int status, String userId, String userNickname

KICK_USERLIST (77) — KickUserListEvent
  List<KickedUser> kickedUsers

ADMIN_CHAT_USER (78) — AdminChatUserEvent
  String type, List<AdminChatUserEntry> users

CLI_DOBAE_INFO (79) — CliDobaeInfoEvent
  int dobaeInfo, String userId

KICK_MSG_STATE (90) — KickMsgStateEvent
  String chatNo, boolean isHideKickMessage

SET_ADMIN_FLAG (126) — SetAdminFlagEvent
  String flag

ADMIN_CHUSER_EXTEND (128) — AdminChuserExtendEvent
  (공통 필드만)

### ItemBaseEvent (아이템)

OGQ_EMOTICON (109) — OGQEmoticonEvent
  String chatNo, String message, String groupId, String subId, String version, String userInfo, String color, String chatLang, String type

SEND_QUICK_VIEW (45) — QuickViewEvent
  String senderId, String senderNickname, String receiverId, String receiverNickname, int itemType

BUY_GOODS (70) — BuyGoodsEvent
  int goodsType, String bjId, String buyerId, String buyerNickname, String goodsName, int goodsCount, int relay

BUY_GOODS_SUB (71) — BuyGoodsSubEvent
  int goodsType, String bjId, String buyerId, String buyerNickname, String goodsName, int goodsCount, int relay

PAID_ITEM (29) — PaidItemEvent
  int itemType, String bjId, String buyerId, String buyerNickname, String itemName, int itemCount

ITEM_DROPS (111) — ItemDropsEvent
  String bjId, String dropsName, String dropsMsg, String dropsImgUrl

ITEM_USING (47) — ItemUsingEvent
  String bjId, int itemType, int remainTime

FOLLOW_ITEM (91) — FollowItemEvent
  int chatNo, String recvId, String sendId, String sendNick, int type

FOLLOW_ITEM_EFFECT (93) — FollowItemEffectEvent
  String bjId, String sendId, String sendNick, int month, int chatNo

MISSION (121) — MissionEvent
  Map<String, Object> data

MISSION_SETTLE (125) — MissionSettleEvent
  Map<String, Object> data

AD_IN_BROAD_JSON (119) — AdInBroadJsonEvent
  Map<String, Object> data

BJ_STICKER_ITEM (36) — BjStickerItemEvent
  int type

BASEBALL_EVENT (28) — BaseballEventEvent
  String eventData

ITEM_STATUS (46) — ItemStatusEvent
  (공통 필드만)

ITEM_SELL_EFFECT (92) — ItemSellEffectEvent
  (공통 필드만)

UPDATE_TICKET (42) — UpdateTicketEvent
  (공통 필드만)

USE_QUICK_VIEW (48) — UseQuickViewEvent
  (공통 필드만)

### NotificationBaseEvent (알림)

NOTICE (10) — NoticeEvent
  String message

BJ_NOTICE (104) — BjNoticeEvent
  int show, String message

LIVE_CAPTION (122) — LiveCaptionEvent
  String caption

SLOW_MODE (23) — SlowModeEvent
  int autotime, int manualtime

NOTIFY_POLL (50) — NotifyPollEvent
  int status, String bjId, int no, int show

TRANSLATION (95) — TranslationEvent
  int idx, int mode, String message, int orgLanguage, int transLanguage

TRANSLATION_STATE (94) — TranslationStateEvent
  int state

### UnknownEvent

NONE_TYPE (-1) — UnknownEvent
  int code, String originalMessage

## Fixture 검증 데이터 (30개 이벤트 실제 캡처)

실제 수집된 패킷 `.bin` fixture를 사용해 디코딩이 검증된 30개 이벤트의 실제 필드 값 예시입니다.
`lib/src/test/java/.../decoder/FixtureDecoderDetailTest.java`에서 각 이벤트별 단위 테스트를 확인할 수 있습니다.

| 이벤트 | 검증된 필드 값 예시 |
|--------|---------------------|
| ADCON_EFFECT | adconCount=200, message="애드벌룬 200개 선물!", urlDefault="https://res.sooplive.com/..." |
| BAN_WORD | replaceWord="***" (대체어), banWordList=["***..."] (금지어 배열, 1개) |
| BJ_NOTICE | show=1, message="***" (공지 텍스트, HTML 허용) |
| BJ_STICKER_ITEM | type=4 |
| CHAT_MESSAGE | type=0(일반), chatLang=3, subscriptionMonth="-1"(비구독), senderFlag="81952|32768" |
| CHAT_USER | type=1(입장), userList.size=40 (일괄 전달), 각 유저: id, nickname, flag |
| CHUSER_EXTEND | userStatus.size=13, 키 형식="fw=N&afw=N" (팔로우/실제팔로우 수) |
| EMOTICON_TICKET | value=1 (채널 입장 직후) |
| FOLLOW_ITEM | type=111, chatNo=7450, recvId=BJ ID, sendId=구독자 ID |
| FOLLOW_ITEM_EFFECT | month=27 (연속 구독 개월), chatNo=2300 |
| ICE_MODE | iceMode=1 (1=활성화) |
| ICE_MODE_EX | iceMode=1, freezeType=1008, balloonLimitCount=1, subscriptionLimitCount=1 |
| ITEM_DROPS | dropsName="컬쳐랜드 1만원", dropsMsg="" (선택), dropsImgUrl="" (선택) |
| JOIN_CHANNEL | maxSubBjCount=10, familyNickname="" (패밀리방 시 값), userFlag="16|16384" |
| KICK_MSG_STATE | chatNo="****" (채팅방 번호), isHideKickMessage=false |
| LOGIN | userId="" (익명=빈 문자열), userFlag="16|0" |
| MISSION | data: {type="CHALLENGE_NOTICE", mission_status="FAIL", title="...", key=숫자, uuid="..."} |
| MISSION_SETTLE | data: {chno=****, fanOrder=****, list=[["userId","닉네임",금액,...]], uuid="..."} |
| OGQ_EMOTICON | groupId="***" (이모티콘 그룹 UUID), subId="3", version="1", userInfo=userId, color=닉네임 |
| OGQ_EMOTICON_GIFT | ogqTitle="***" (스티커팩 이름), ogqImageUrl="//ogq-sticker-global-cdn-z01.sooplive.com/..." |
| SEND_BALLOON | count=3000, fileName="3000"(효과파일명), isDefault=false, ttsData="yumi"(TTS캐릭터) |
| SEND_QUICK_VIEW | itemType=101, senderId/receiverId 쌍으로 전달 |
| SEND_SUBSCRIPTION | itemType=11, itemCode="***" (서버 생성 구독 코드), isSubscription=0, subscriptionType="0" |
| SET_BJ_STAT | 공통 필드만 (방송 연결 해제 신호) |
| SET_DUMB | dumbTime=30(초), dumbCount=1, adminType=1(방송인) or 2(매니저), extraInfo="" |
| SET_NICKNAME | changeType=0, oldNickname="***", newNickname="***", flag="65536|163840" |
| SET_SUB_BJ | hide=0(0=표시), flag="269025632|688128" |
| SET_USER_FLAG | oldFlag="720928|163840" → newFlag="589856|163840" (입장 등급 변경 시) |
| TRANSLATION_STATE | state=1 (1=번역 활성화, 0=비활성화) |
| VIDEO_BALLOON | balloonCount=9, isDefault=true, relay="0", extraData=MD5해시 |

### 코드표 및 플래그 해석

`senderFlag`, `userFlag`, `flag` 등은 "주|보조" 형식의 문자열로 전달되며, 파이프(`|`)로 구분된 두 정수는 서로 다른 비트 의미를 가집니다.

`com.github.getcurrentthread.soopapi.code` 패키지가 이 원시 값들을 타입으로 디코딩합니다. 원시 필드(String/int)는 그대로 유지하고, 이벤트 Record에 추가된 접근자가 **호출 시점에 지연 파싱**합니다(디코딩 경로 불변).

- `UserLevel.parse(flag)` → `UserLevel(Set<UserFlag> primary, Set<UserFlag2> secondary)`.
  - 주 그룹 `UserFlag`: ADMIN=1, HIDDEN=2, BJ=4, DUMB=8, GUEST=16, FANCLUB=32, MANAGER=256, FEMALE=512, MOBILE=16384, TOPFAN=32768, REALNAME=65536, QUICKVIEW=1<<19, FOLLOWER=1<<28, NOTIVODBALLOON=1<<30, NOTITOPFAN=1<<31 …
  - 보조 그룹 `UserFlag2`: GLOBAL_PC=1, CLAN=2, TOPCLAN=4, GAMEGOD=16, ADMINCHAT=8192, PC=16384, SPECIFY=32768, HTML5=1<<17 …
  - 두 그룹은 비트값이 겹치므로(예: 16 = GUEST(주) vs GAMEGOD(보조)) 반드시 각자의 enum으로 분해합니다. 부호 비트(NOTITOPFAN=1<<31)는 `(mask & bit) == bit`로 안전하게 처리됩니다.
  - 접근자: `ChatMessageEvent.senderLevel()`, `ChatUserEntry.level()`, `AdminChatUserEntry.level()`, `LoginEvent.userLevel()`, `JoinChannelEvent.userLevel()`, `SetUserFlagEvent.oldLevel()/newLevel()`, `SetAdminFlagEvent.level()`.
- 아이스(채팅 제한) 모드: `ChatIceType.fromCode(int)`(레거시 0~4: CLEAR/NORMAL/FAN/SUP/FAN_SUP) + `ChatIceType.Flag.fromMask(int)`(v2 비트: NORMAL2=16, FAN2=32, SUP2=64, TOP_FAN2=128, FOLLOWER2=256). 접근자: `IceModeEvent`/`IceModeExEvent`/`GetIceModeRelayEvent` 의 `iceType()`, `iceFlags()`, `isIceFlagMode()`.
- 채널 퇴장 사유: `ChatQuitStatus.fromCode(int)` (0=CLOSED, 1=NORMAL, 2=KCIK, 3=DUMB, 4=OVERCHAT, 5=BLIND, 6=ADMKICK). 접근자: `QuitChannelEvent.quitStatus()`.

알 수 없는 코드는 센티넬(`UNKNOWN` / `UserLevel.EMPTY`)을 반환하며 예외를 던지지 않습니다.

### JSON 이벤트 (Map<String, Object>)

MISSION, MISSION_SETTLE, AD_IN_BROAD_JSON 이벤트는 `data` 필드에 JSON 파싱 결과를 담습니다.
JSON 숫자는 Gson의 기본 동작에 따라 `Double`로 파싱될 수 있습니다.

## 이벤트 시스템 내부

### EventEmitter 구현

파일: event/EventEmitter.java

- listeners: ConcurrentHashMap<ChatEvent, CopyOnWriteArrayList> — 사용자 리스너
- internalListeners: 같은 구조 — 내부 전용 (registerEnterInfoHandler 등)
- onceWrappers: ConcurrentHashMap<원본리스너, 래퍼리스너> — once() 추적
- emit() 순서: internalListeners 먼저, 그 다음 listeners
- 에러 처리: 리스너 예외 catch → errorHandler 호출 → errorHandler 자체 예외도 catch

### once() 구현

래퍼 패턴: 실행 시 off(event, this) 호출 후 원본 리스너 실행.
onceWrappers 맵으로 원본↔래퍼 매핑 추적 (off() 시 래퍼도 제거 가능).

### Sealed Event 계층 상세

```java
public sealed interface BaseEvent
        permits ChatBaseEvent, DonationBaseEvent, SystemBaseEvent,
                ModerationBaseEvent, ItemBaseEvent, NotificationBaseEvent,
                UnknownEvent {
    ChatEvent eventType();
    String raw();
    long timestamp();
}
```

각 서브타입은 non-sealed 인터페이스로, 여러 Record가 implements 가능.

## 테스트

### 테스트 클래스 (13개)

| 테스트 | 대상 | 유형 |
|--------|------|------|
| SOOPChatClientTest | 채팅 클라이언트 | 컴포넌트 |
| SOOPChatClientConcurrencyTest | 동시성 동작 | 컴포넌트 |
| SOOPChatClientRealConnectionTest | 실제 연결 | 통합 (@Tag) |
| SOOPClientConfigTest | 설정 빌더 검증 | 단위 |
| ChatMessageDecoderTest | 메시지 디코딩 | 단위 |
| DecoderBoundsValidationTest | 배열 범위 검증 | 단위 |
| MessageDispatcherTest | 메시지 라우팅 | 컴포넌트 |
| EventEmitterTest | 이벤트 구독/발행 | 컴포넌트 |
| EventEmitterOnceOffTest | once() 메모리 정리 | 컴포넌트 |
| GsonUtilTest | JSON 직렬화 | 단위 |
| SOOPChatUtilsTest | 유틸리티 파싱 | 단위 |
| WebSocketListenerBufferTest | 텍스트 버퍼 | 단위 |
| WebSocketPacketBuilderTest | 패킷 직렬화 | 단위 |

### 테스트 실행

```bash
./gradlew :lib:test                    # 단위 + 컴포넌트 (통합 제외)
./gradlew :lib:test -PincludeTags=integration  # 통합 포함
```

### 테스트 규약

- JUnit 5 (Jupiter) + Mockito 5.14.2
- @Tag("integration")로 통합 테스트 분리 (CI에서 기본 제외)
- 동시성 테스트: CountDownLatch, AtomicReference, AtomicInteger

## 빌드 시스템

### Gradle 설정 (lib/build.gradle)

- 플러그인: java-library, maven-publish, spotless(8.2.1)
- Java 25 toolchain
- 인코딩: UTF-8
- Spotless: Google Java Format (AOSP variant)
- import 순서: java, javax, jakarta, org, com
- JavaCompile 시 spotlessApply 자동 실행

### CI (GitHub Actions)

파일: .github/workflows/ci.yml

- 트리거: push/PR to develop, main
- 환경: ubuntu-latest, Oracle JDK 25
- 단계: checkout → setup-jdk → setup-gradle → build → publishToMavenLocal 검증

### JitPack 배포

파일: jitpack.yml

- openjdk25 설정
- publishToMavenLocal -xtest -xspotlessApply

### 주요 Gradle 명령

```bash
./gradlew build                    # 컴파일, 테스트, 패키지
./gradlew :lib:build               # 라이브러리 모듈만
./gradlew :lib:publishToMavenLocal # 로컬 Maven 배포
./gradlew spotlessApply            # 코드 포맷팅
```

## 기여 가이드

### 새 이벤트 추가 절차

1. ChatEvent enum에 새 코드 추가 (필요 시)
   - 파일: event/ChatEvent.java
   - 적절한 카테고리 주석 아래에 추가

2. event/model/ 에 Record 생성
   - 적절한 BaseEvent 서브타입 implements (ChatBaseEvent, DonationBaseEvent 등)
   - 반드시 eventType(), raw(), timestamp() 필드 포함

3. decoder/message/ 에 IMessageDecoder 구현 클래스 생성
   - MIN_PARTS로 최소 필드 수 검증
   - parts[] 인덱스로 필드 매핑
   - 선택적 필드는 parts.length > N 패턴 사용

4. decoder/factory/DefaultMessageDecoderFactory 에 Map.entry() 추가

5. 테스트 작성: 디코더 단위 테스트

### 새 API 엔드포인트 추가

1. api/ 패키지에 메서드 추가 (SOOPAuth, SOOPLive, SOOPChannel 중)
2. api/model/ 에 response record 생성
3. SOOPClient에 접근자 추가 (필요 시)

### 코드 스타일

- Google Java Format (AOSP variant) — Spotless 자동 적용
- import 순서: java, javax, jakarta, org, com
- 불필요한 import 자동 제거
- 후행 공백 제거, 파일 끝 개행

### 브랜치 전략

- develop: 주 개발 브랜치 (PR 대상)
- main: 릴리스 브랜치

## 성능 고려사항

### 메모리 최적화

- SHARED_DECODERS: SOOPConnection의 static final — 모든 연결에서 디코더 인스턴스 공유
- WebSocketListener textBuffer: 기본 16KB, 4배(64KB) 초과 시 trimToSize()
- IODH Holder 패턴: lazy 초기화로 불필요한 인스턴스 생성 방지 (SSLContextProvider, ConnectionManager)
- utf8ByteLength(): 바이트 배열 할당 없이 문자열 UTF-8 길이 계산
- RAW 이벤트: hasListeners() 체크 후 할당 (리스너 없으면 RawEvent 생성 안 함)
- hasListeners() 조기 반환: 리스너 없으면 디코딩 자체를 스킵

### 동시성

- Virtual Thread per task executor (messageProcessorPool)
- ConcurrentHashMap + CopyOnWriteArrayList: lock-free 이벤트 발행
- AtomicReference<WsState>: WebSocket 상태 원자적 관리
- ReentrantLock: 연결 상태 보호 (connect/disconnect/reconnect)

## 버전 관리

현재 버전은 상단 **핵심 정보**와 `gradle.properties`의 `version` 값을 참조하세요.

- `gradle.properties`의 `version`으로 관리
- JitPack은 git 태그(`vX.Y.Z`)로 빌드

### 버전 업데이트 절차

릴리스는 `.github/workflows/release.yml`이 자동화합니다(`workflow_dispatch`로 버전 입력 또는 `v*` 태그 push 시 트리거).
워크플로가 README.md·gradle.properties·llms.txt·llms-full.txt의 버전을 일괄 갱신하고 직전 커밋에 amend한 뒤, 태그를 생성·푸시하고 GitHub Release를 만듭니다.

## 면책 조항

이는 비공식 API이며 SOOP와 제휴되거나 승인되지 않았습니다. 사용에 따른 책임은 사용자에게 있습니다.
SOOP 플랫폼의 웹소켓 통신 방식이 변경되면 동작하지 않을 수 있습니다.
